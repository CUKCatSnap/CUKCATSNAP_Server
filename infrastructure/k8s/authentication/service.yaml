apiVersion: v1
kind: Service
metadata:
  name: catsnap-authentication
  labels:
    app: catsnap-authentication
spec:
  type: ClusterIP
  selector:
    app: catsnap-authentication
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080