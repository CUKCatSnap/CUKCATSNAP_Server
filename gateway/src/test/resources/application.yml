server:
  port: 8000

spring:
  application:
    name: gateway
  security:
    jwt-key: testessztest123dsaqwgqtestestwqw
  cloud:
    gateway:
      routes:
        - id: mono-route
          uri: http://localhost:8080
          predicates:
            - Path=/**
