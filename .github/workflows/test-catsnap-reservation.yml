name: Test Reservation Module

on:
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Gradle Caching
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Test reservation module
        run: ./gradlew --info :catsnap-reservation:test

      - name: Publish test results for reservation
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          files: 'catsnap-reservation/build/test-results/test/TEST-*.xml'
          check_name: Test Results (catsnap-reservation)

      - name: Publish test report for reservation
        uses: mikepenz/action-junit-report@v4
        if: always()
        with:
          report_paths: 'catsnap-reservation/build/test-results/test/TEST-*.xml'
          check_name: Test Report (catsnap-reservation)

      - name: Upload coverage reports to Codecov for reservation
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: catsnap-reservation/build/reports/jacoco/test/jacocoTestReport.xml
          flags: catsnap-reservation
          name: catsnap-reservation-coverage