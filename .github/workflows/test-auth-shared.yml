name: Test Auth Shared Module

on:
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Gradle Caching
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Test auth-shared module
        run: ./gradlew --info :auth-shared:test

      - name: Publish test results for auth-shared
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          files: 'auth-shared/build/test-results/test/TEST-*.xml'
          check_name: Test Results (auth-shared)

      - name: Publish test report for auth-shared
        uses: mikepenz/action-junit-report@v4
        if: always()
        with:
          report_paths: 'auth-shared/build/test-results/test/TEST-*.xml'
          check_name: Test Report (auth-shared)

      - name: Upload coverage reports to Codecov for auth-shared
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: auth-shared/build/reports/jacoco/test/jacocoTestReport.xml
          flags: auth-shared
          name: auth-shared-coverage