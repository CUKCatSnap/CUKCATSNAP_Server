name: Test Common Module

on:
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Gradle Caching
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Test common module
        run: ./gradlew --info :common:test

      - name: Publish test results for common
        uses: EnricoMi/publish-unit-test-result-action@v2
        if: always()
        with:
          files: 'common/build/test-results/test/TEST-*.xml'
          check_name: Test Results (common)

      - name: Publish test report for common
        uses: mikepenz/action-junit-report@v4
        if: always()
        with:
          report_paths: 'common/build/test-results/test/TEST-*.xml'
          check_name: Test Report (common)

      - name: Upload coverage reports to Codecov for common
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: common/build/reports/jacoco/test/jacocoTestReport.xml
          flags: common
          name: common-coverage